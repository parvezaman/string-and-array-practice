// not solved yet
#include <stdio.h>
#include <string.h>
int main()
{
    char givenString[100], desiredOutput[100];
    gets(givenString);
    int length = strlen(givenString), position = 0;
    for (int i = 0; i < length; i++)
    {
        if (givenString[i] = '.')
        {
        }
    }

    return 0;
}